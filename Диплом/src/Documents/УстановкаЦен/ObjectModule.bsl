
Процедура ОбработкаПроведения(Отказ, Режим)
	
	Движения.ЦеныНоменклатуры.Записывать = Истина;
	Для Каждого ТекСтрокаСписокНоменклатуры Из СписокНоменклатуры Цикл
		Движение = Движения.ЦеныНоменклатуры.Добавить();
		Движение.Период = Дата;
		Движение.Наименование = ТекСтрокаСписокНоменклатуры.Наименование;
		Движение.ЗакупочнаяЦена = ТекСтрокаСписокНоменклатуры.ЗакупочнаяЦена;
		Движение.РозничнаяЦена = ТекСтрокаСписокНоменклатуры.РозничнаяЦена;
	
	КонецЦикла;

КонецПроцедуры
